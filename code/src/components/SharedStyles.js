import { css } from "styled-components";

//styled components provides an opportunity to destructure styling and take repetitions into a separate component

export const SharedStyles = css`
  top: 0;
  left: 0;
  box-sizing: border-box;
  display: flex;
  padding: 20px;
`;
